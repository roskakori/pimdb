version: "3.7"

services:
  postgres:
    image: "postgres"
    container_name: "pimdb_postgres"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - 5439:5432

volumes:
  postgres-data:
    driver: local
